/**
 * PipPlace icon: thin wrapper around the shared icon language
 * (see @/shared/components/ui/game-icon).
 */

import { createGameIcon, GAME_ICON_ART } from '@/shared/components/ui/game-icon'

export const PipPlaceIcon = createGameIcon(GAME_ICON_ART['pip-place'])
