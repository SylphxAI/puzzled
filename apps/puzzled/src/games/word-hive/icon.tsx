/**
 * WordHive icon: thin wrapper around the shared icon language
 * (see @/shared/components/ui/game-icon).
 */

import { createGameIcon } from '@/shared/components/ui/game-icon'

export const WordHiveIcon = createGameIcon('word-hive')
