# S5 recovery run — #153 rebase+budgets (started 2026-09-21 ~00:35BST by recovery agent)
- Step 0: remote confirmed: main=7523ba9ef74eb80567a44b033b02394f91cf28a0, s2/quality-gates=90e2a679e9d5c3b9627ab4dff61f13260a1517e2.
- Worktree reuse: /data/sylphx/home/work/pz-quality-gates (clean, branch s2/quality-gates @ 90e2a67).
- Next: fetch + rebase onto origin/main; then measure lighthouse mobile+throttling on rebased tree.
- Step 2 done 00:4x: rebase onto origin/main 7523ba9 CLEAN (no conflicts). New head: 3933ad4 (chromium pin), 352d348 (wire gates). Branch local only; committing this note + pushing force-with-lease next.

- Step 3 (design) 00:5x: free rotation verified by executing the repo module: today (HKT 2026-09-21) = sudoku; rotation = word-guess, word-groups, crowns, sudoku, crossword. Anonymous access = slug===today's free game, so ANY rotation slug's rendered branch flips by day; non-rotation slugs always render the gated (unlock) branch. Decision: LH urls = / + /games/word-search + /games/nonogram + /games/arithmo (all non-rotation -> day-deterministic). Local port 3016 because the pod's :3000 is the OpenClaw gateway (CI has :3000 free; final config keeps 'bun run start').
- Step 3 (measure) starting: mobile + default simulated throttling, 3 runs x 4 urls, CHROME_PATH=~/.cache/ms-playwright/chromium-1243/chrome-linux64/chrome (playwright 1.58.2 pinned in bun.lock).

- Step 3/4 DONE 01:2x: measured mobile+throttled (3 runs x 4 urls) at 7c4f16f: perf 0.79 (/) / 0.76 (word-search) / 0.77 (nonogram) / 0.77 (arithmo); LCP 5372 / 5726 / 5881 / 5755 ms; FCP, CLS, TBT, SI pass. LCP element on every route = the consent banner (deferred shell chunk, ~5.4-6.0 s simulated vs 0.54-1.95 s observed). raw: $HOME/work/pz-program/evidence/s5/run1*.
- Step 4 DONE: apps/puzzled/lighthouserc.json corrected (mobile formFactor, no throttling override, urls / + /games/word-search + /games/nonogram + /games/arithmo, explicit assertions perf>=0.9, LCP<=2500, CLS<=0.1, TBT<=300, FCP<=2000, SI<=3000 — all error). ci.yml: only the job's explanatory comment updated to match. No app code touched: the LCP budget cannot pass without app work (the banner must paint in the first paint / be SSR-visible, reversing #144's deliberate deferral) — reported, not papered over.

- Recovery#2 01:10 BST: resumed at b490020 (remote==local, clean). PR checks at resume: Security Scan pass / Migration Integrity pass / Rust API pass / Lint&TypeCheck pending. Mount chain: (main)/layout.tsx -> OverlaysChrome (Suspense, awaits identity) -> LayoutOverlays -> DeferredOverlays -> idle release -> deferred chunk -> ConsentBanner -> SDK CookieBanner. #144=861cfbd; its message says the home hero was repainted "so it is the LCP element instead of the consent bar" — the mobile measurement still shows the banner as LCP on all 4 routes (larger than the fold text AND inserted after idle; both matter).
- Plan: reverse ONLY the consent part of #144's deferral. Mount <ConsentBanner/> eagerly in (main)/layout.tsx OUTSIDE the identity Suspense so its markup flushes with the shell (first paint); keep toaster/monitoring/achievements/PWA deferred; send-site consent gate untouched. Add a [locale]-level pre-paint script: if localStorage['puzzled-consent'] parses, set html[data-consent-decided]; CSS hides [data-consent-banner] so settled users never see a flash and hydration stays consistent. CookieBanner root gets the data-consent-banner hook. react.tsx is already in every route's client graph via PlatformProvider, so this is not a bundle regression.
- Local protocol re-created next (lighthouserc.s5-local.json was lost with /tmp): same as CI config, port 3016, CHROME_PATH pinned; evidence under $HOME/work/pz-program/evidence/s5/run2*.

- Recovery#2 01:35: implemented (8 app files, +83/-17): (main)/layout.tsx mounts <ConsentBanner/> itself, outside the identity Suspense (first flush); deferred-chunk/deferred-shell/layout-overlays drop it; [locale]/layout.tsx head runs a settle script (localStorage['puzzled-consent'] parses -> html[data-consent-decided]); globals.css hides [data-consent-banner] under that attribute; react.tsx gets the data-consent-banner hook on the SDK banner root; docstrings updated. Send-site consent gate untouched; everything else stays deferred.
- Build #1 (build2.log) compiled but emitted STALE CSS for globals.css (rule absent; podium section adjacent to reduced-motion). A standalone postcss+@tailwindcss/postcss compile of globals.css DID emit the rule -> turbopack cache suspicion. Clean rebuild (build3.log, .next removed) emits it: html[data-consent-decided] [data-consent-banner]{display:none} in chunk 3bw4der67822j.css.
- Browser proofs (check2/check5.mjs, chromium-1243): fresh visitor: banner present+visible; Accept -> puzzled-consent={"analytics":true,"marketing":false}, puzzled:consent:cookie='accepted', timestamp set, banner unmounted; settled reload: html attr set pre-paint, banner never paints (styled samples all display:none); invalid JSON: attr absent, banner shown; console errors only 401/502 from absent offline APIs, no hydration errors.
- LH protocol run2 started ~01:4x (lighthouserc.s5-local.json recreated, port 3016, CHROME_PATH pinned).

- Recovery#3 02:3x: verified run2 evidence == tree (src mtimes 01:19 < BUILD_ID 01:29 < build3.log 01:29:40 < server3.log 01:30:35 < run2 LHRs 01:31-01:34). No stale servers on 3016; started fresh server (next start -p 3016, /tmp/srv9.log).
- Root-caused the residual LCP: the banner IS in the SSR HTML (data-consent-banner at byte 54.7K/382K = 14.3%) and paints at FCP time observed (single LH run w/ --save-assets: observedLCP = observedFCP = observedFP = 288ms, one LCP candidate). But lantern SIMULATED LCP = 4541ms. Mechanism (lighthouse trace_engine lantern LargestContentfulPaint/FirstContentfulPaint.getFirstPaintBasedGraph): the LCP graph includes EVERY network node that ended before the observed LCP paint (cutoff=288ms) - locally that is 31 requests / 587,736 bytes (24 script chunks ~430KB + 2 fonts 78KB + 2 css 30KB + doc + manifest) - all charged as render-blocking against a 1.6Mbps simulated link. FCP only charges doc+CSS (1366ms). So LCP budgets = "everything loaded before first paint must fit mobile sim".
- Next: LANTERN_DEBUG=1 lh run to dump per-node simulated timings (which node is the critical path). Then cut pre-paint payload (font preloads / chunks) and re-measure.

- Recovery#3 02:5x — ROOT CAUSE NAILED (lantern debug traces via LANTERN_DEBUG=1 + --save-assets):
  * Sim LCP = max simulated end over EVERY network node that ended before the observed LCP paint (FirstContentfulPaint.getFirstPaintBasedGraph, cutoff=observed LCP). FCP's graph excludes script-initiated nodes; LCP's graph does NOT. Observed FCP = LCP = 243-288ms (banner indeed paints at first paint now), but sim charges all 31 pre-paint requests / 587,736 bytes: 24 JS chunks (~430KB) + 2 fonts (79KB) + 2 css (30KB) + doc (67KB) + manifest. Serialized over the 1.6Mbps mobile sim the tail node is 3vqo3d7qp4snm.js (BottomNav) ending at 4524ms = the metric.
  * In-app cuts cannot find 2s: the LIGHTEST route (/unsubscribe, ~21 pre-paint nodes) measured with the EXACT lhci toolchain (2 runs, same mobile settings): LCP 3619.12 / 3178.68 -> median 3178.68, FAIL vs <=2500. Log: pz-program/evidence/s5/lhci-unsubscribe-floor.log. Game routes (~31 nodes) = 4135-4541 (run2 + this session's single run 4541).
  * Verdict: LCP<=2500 @lhci mobile-sim sits BELOW the harness floor for this app/stack (~3.2s even for the lightest page; framework+fonts+css+doc chain alone is charged ~2.9-3.5s in the sim traces). Banner fix kept (real 1.3s improvement); budget/profile decision needed from owner: (a) raise LCP budget to measured floor, (b) method change with data (devtools-throttling run in flight), (c) large deferred-JS program (estimated still ~3.0-3.5s).
  * App fix committed as WIP so it survives pod death (budgets still red; not a green-wash: numbers above).
