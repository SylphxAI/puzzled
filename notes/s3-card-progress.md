# s3-card (result card — S3 slice 2, register row G3) — progress

**Task:** implement the non-spoiler RESULT CARD for apps/puzzled (pure model + canvas PNG renderer + one-tap share wiring + i18n x5 + a11y/reduced-motion), with tests, rendered evidence, PR. Base: origin/main = 7523ba9. Worktree: $HOME/workspace/.worktrees/github.com/SylphxAI/puzzled/s3-card; branch s3/result-card. Prefix git/gh with env -u GH_TOKEN -u GITHUB_TOKEN.

**Off-limits:** src/features/home/**, apps/puzzled/lighthouserc.json, .github/**, other register rows, other PRs' files, src/games/*/translations/**. No merge, no enqueue. Avoid PR #153 files (.github/workflows/ci.yml, e2e-tests/a11y-support.ts, e2e-tests/accessibility.e2e.ts, notes/s5-rebase-progress.md).

## Log

- [2026-09-21 ~00:50] **Recon done.** Worktree s3-card exists at 7523ba9 (clean). Only open PR: #153. Sibling branches s3-day / td-ssot / zh-games add only notes/AGENTS so far (no overlap). bun install started in worktree (shell session cool-coral, log /tmp/s3card-install.log).
- Context read: protocol section 6; s3-direction.md; gaps.md G3 row; share-text.ts + tests; game-result.tsx + modal; theme-colors.ts (11 themes, Tailwind classes); request.ts (per-locale namespaces; Messages = Record<string,unknown>); already-completed-view.tsx (own share handler; NOT in this slice's named wiring files).
- **Scope frozen:** NEW src/features/daily/lib/result-card.ts (model + text alternative; typed tile pattern, no free text); NEW result-card-render.ts (canvas 1080x1080, common chrome, theme palettes; paint + blob separated for testability); NEW share-result-card.ts (pure orchestration: files-capable share -> navigator.share; else download + text copy fallback; cancel handled); tests for all three. MOD game-result.tsx (new 'Share result card' primary button + optional props puzzleDate/currentStreak/pattern; existing text share kept); game-result-modal.tsx pass-through. Thread puzzleDate={puzzleDate} through all 21 GameResultModal call sites + scan guard test. i18n: extend src/messages/*/share.json (5 locales) with card strings + parity guard test. Update gaps.md G3 row ONLY (closed-at-source, live layer Unknown). Evidence: playwright chromium runs the real renderer bundle -> real PNG under $HOME/work/pz-program/evidence/s3-card/.
- Day fallback: prefer prop puzzleDate; else productDayKey() for daily mode; else omit. Pattern = typed tiles (hit/near/miss) so no answer text can ride the model.
- **Next:** commit this note + push; checkpoint 1 = model + tests (+ mutation proof).
- [2026-09-21 ~01:15] **Checkpoint 1/5 done: model + tests.** New: src/features/daily/lib/result-card.ts (buildResultCard field-by-field allowlist; typed tiles hit/near/miss capped 12x12; time bands; day via isValidDayKey + zone-stable Intl; deep link reuses ritualShareDeepLink; resultCardChips; resultCardTextAlternative) + result-card.test.ts. Suite: 13 pass / 0 fail. Mutation proof: spread-leak injected -> 12 pass / 1 fail, 'CROWN' visible in received payload; reverted -> 13/0 and file byte-identical. Evidence: $HOME/work/pz-program/evidence/s3-card/mutation-red.txt + mutation-green.txt.
- [2026-09-21 ~01:18] **LESSON: git push needs the env prefix too.** A push without `env -u GH_TOKEN -u GITHUB_TOKEN` failed 403 (denied to shtse8); with the prefix it succeeded. Every git/gh call gets the prefix.
- **Next:** checkpoint 2 = canvas renderer (result-card-render.ts) + PNG evidence via real chromium.
- [2026-09-21 ~01:25] **Checkpoint 2/5 done: renderer + real PNG evidence.** New: src/features/daily/lib/result-card-render.ts (RESULT_CARD_SIZE 1080; paintResultCard(ctx, model, strings) DOM-free/deterministic; resultCardBlob via canvas.toBlob; resultCardFileName; 11 theme palettes cross-checked against theme-colors rgba triples) + result-card-render.test.ts (14 pass, recording 2D context; no solution text can reach fillText; one common chrome across themes). Model tweak: time-band chip (strings.timeLabel) + updated model tests, now 14 pass.
- Evidence: $HOME/work/pz-program/evidence/s3-card/ - card-word-guess-2026-09-21.png (798681 bytes, md5 87a1c1...) and card-crossword-zh-HK-2026-09-21.png (810431 bytes); command recorded in that dir's README.md (bun build entry.ts --format=iife + chromium-1243 via playwright render.mjs). Pixel checks with ImageMagick: 19396 exact emerald #10B981 pixels; 1560 blue #3B82F6; title band mean 0.215 vs 0.120 background.
- Gotcha: bun build `--global-name` emitted no assignment in 1.4.2 - entry assigns globalThis.S3Card itself. Chromium lives at chromium-1243/chrome-linux64/chrome (not chrome-linux).
- **Next:** checkpoint 3 = share wiring on game-result surfaces (share-result-card.ts + GameResultCard button + puzzleDate thread-through) + wiring tests.
- [2026-09-21 ~01:40] **Checkpoint 3/5 done: one-tap share wiring.** New: src/features/daily/lib/share-result-card.ts (shareResultCard decision table with injected navigator/document/URL/render seams: files-capable -> navigator.share; abort -> cancelled; otherwise download + caption copy; honest 'unavailable'; shareRitualResultCard renders then shares) + share-result-card.test.ts (12 pass). MOD game-result.tsx: new primary 'Share result card' button (tShare card.* strings, busy state, <output> notice, sr-only text alternative) with the existing text share kept as an outline button; optional props puzzleDate/currentStreak/pattern; day falls back to productDayKey() only for daily mode. MOD all 19 <GameResultModal> call sites (script $HOME/work/pz-program/evidence/s3-card/thread-puzzledate.mjs) to pass puzzleDate={puzzleDate}; word-guess passes a real pattern from its evaluations (statuses only). NEW guard src/games/shared/result-card-wiring.test.ts (19 call sites dated; surface keeps both shares; modal passes props through).
- Checks: tsc --noEmit rc=0; bun test src/features/daily src/games/shared src/lib/i18n -> 219 pass / 0 fail (22 files).
- **Next:** checkpoint 4 = i18n card.* in all five share.json + parity guard; then re-render PNG from the real catalogue strings; then G3 row (checkpoint 5) + PR.
